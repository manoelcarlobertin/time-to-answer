<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">Excluindo Administrador</h1>
  </div>
</div>

<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Confirmação de Exclusão</h3>
      </div>
      <div class="panel-body">
        <div class="alert alert-warning">
          <i class="fa fa-exclamation-triangle"></i>
          Tem certeza que deseja excluir este administrador? Esta ação não pode ser desfeita.
        </div>
        <div class="row">
          <div class="col-lg-6">
            <%= form_with(model: [:admins_backoffice, @admin], url: admins_backoffice_admin_path(@admin), html: { class: "form-horizontal" }, local: true) do |form| %>
              <%= form.hidden_field :_method, value: :delete %>

              <div class="form-group">
                <%= form.label :email, 'E-mail do Administrador' %>
                <%= form.text_field :email, class: "form-control", readonly: true %>
              </div>

              <div class="form-actions">
                <%= button_tag type: 'submit', class: "btn btn-danger" do %>
                  <i class="fa fa-trash"></i> Excluir
                <% end %>
                <%= link_to admins_backoffice_admins_path, class: "btn btn-default" do %>
                  <i class="fa fa-arrow-left"></i> Voltar
                <% end %>
              </div>
            <% end %>
          </div>        </div>
      </div>
    </div>
  </div>
</div>
